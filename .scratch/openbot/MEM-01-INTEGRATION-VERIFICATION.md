# MEM-01 final integration evidence

Source commit: `19568c9fc603b97213cd5560a097471ba93107fa`.
Reviewed and verified tree: `0f33d9b41358f50a67ebafc0c5f0e6a43fe625d1`.
Merge parents: MEM `44606fef5b18eb668e40b4f97655e09009c8b022` and complete COL-05 `9425c6647869668bc6de9112349d69219cd40131`.

Root reported independent Spec CLEAN and the independently assigned reviewer reported Standards CLEAN for this exact source tree. Root authorized the source commit followed by a separate documentation-only evidence commit. The original six acceptance criteria in ticket 37 are unchanged. The dedicated combined merge and actual native/Compose gates remain separate from these source reviews.

## Final gates on the reviewed tree

Worktree: `/workspace/scratch/2bc98607b3a9/openbot/.worktrees/mem-01`.
Evidence log directory: `/workspace/scratch/2bc98607b3a9`.

| Gate                                                   | Command and working directory                                                                                    | Actual result                                                                                                                                                       | Log                              |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| Formatting, types, unit, integration, production build | `pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:integration && pnpm build` from worktree             | Exit 0; API unit 122, Web unit 126, API integration 401, Web integration 617: **1,266 passed**. API/Web types and builds passed; Web reported zero errors/warnings. | `mem01-final-noport.log`         |
| Ordinary browser and OIDC                              | `pnpm test:e2e` from `apps/web`                                                                                  | Exit 0; **46 ordinary passed**, followed by **1 OIDC passed**                                                                                                       | `mem01-final-browser.log`        |
| Exact source integrity                                 | `git diff --exit-code 0f33d9b41358f50a67ebafc0c5f0e6a43fe625d1 --` and `git diff --cached --check` before commit | Exit 0; committed tree equals the reviewed tree                                                                                                                     | Git tree/commit above            |
| Browser lease closure                                  | Bounded Node TCP connects to `127.0.0.1:4399` and `127.0.0.1:4173` after browser exit                            | Both `ECONNREFUSED`; explicitly released to root and dedicated merger                                                                                               | Reported directly to both owners |

No source edits occurred during either final test chain. The older `mem01-integrated-browser-final.log` proves a superseded tree only; it is not the final `0f33d9b` evidence.

## Witnessed failures and corrections

| Run                                        | Actual result                                                | Interpretation or correction                                                                                                                                                                                                                      |
| ------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Initial integrated source/UI focused tests | 3 failed, 13 passed; exit 1, then 16 passed; exit 0          | Wrong group/direct form paths could create before route validation; live-only source lacked an initial server command map. The BFF now checks the route group before POST, and Save uses the live command mapping.                                |
| Non-enhanced search loader regression      | 1 failed, 13 passed; exit 1, then combined 22 passed; exit 0 | Successful `?/search` action reloaded through a rejecting query parser. Only the exact empty search action marker is now admitted by the list loader; malformed/foreign/duplicate markers remain rejected.                                        |
| `mem01-integrated-browser-focused.log`     | 7 passed, 1 failed; exit 1                                   | Test opened Save before the source edit collapsed its details. Current source/key assertions were correct; test now opens after the edit. Corrected case passed 1/1 in `mem01-integrated-browser-stream-recheck.log`, exit 0.                     |
| `mem01-integrated-browser-full.log`        | 43 passed, 1 failed; exit 1; OIDC not run                    | ATT's broad status locator also matched the new stream status. Test now selects the exact successful purge text; production purge had succeeded and redacted the message.                                                                         |
| `mem01-integrated-browser-final.log`       | 44 ordinary passed and 1 OIDC passed; exit 0                 | Superseded tree `868781c9f3217224764582dadd577e7e3633e41f`, before the final retry correction.                                                                                                                                                    |
| `mem01-command-retry-red.log`              | 2 failed; exit 1                                             | Invalid fixture column `version` prevented the intended probe. Fixed to persisted `message_version AS version`; this run is **not** meaningful production RED evidence.                                                                           |
| `mem01-command-retry-red-confirmed.log`    | 2 failed; exit 1                                             | Real API commits completed through `route.fetch()`, responses were aborted, then disconnect/expiry caused bootstrap. Save changed its key/confidence and stored two memories; edit changed its key/expected version and stored an extra revision. |
| `mem01-command-retry-green.log`            | 2 passed; exit 0                                             | The same actual browser flow retains submitted fields after reset. SQL inspection finds one saved memory with the original key/source/confidence and exactly one edit revision with the original edit key.                                        |
| Final no-port/browser logs above           | All gates passed; exit 0 for each complete chain             | Re-runs on the final reviewed source include both retry cases and the route-scope, live-source, non-enhanced search and ATT locator regressions.                                                                                                  |

The two final retry cases are in `apps/web/tests/e2e/conversation-command-retry.spec.ts`; the stream fixture adds only bounded read-only state needed to inspect actual persisted commands/revisions. The production fix is the scoped snapshot helper plus shared conversation page, not fixture-only handling. API permission and current-source checks still execute on retries.

## Shared integration intent

| Paths                                                                                                                                              | Preserved behavior for the dedicated merger                                                                                                                                                                                                           |
| -------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/api/src/tasks/queue.ts`                                                                                                                      | Identified memory contribution and aggregate limits at claim; append-only exact source manifest after successful CAS; fresh manifest checks before delta and inside private `finishTransaction`; complete COL-05 deadline rollback/re-admission path. |
| `apps/api/src/conversations/append-event.ts`, `stream-schema.ts`, `stream-*`                                                                       | Typed single-namespace delivery, current-source reads, deferred tail prefix guard, deadline checks and bounded retention. Memory introduces no alternate allocator or generic event writer.                                                           |
| `apps/api/src/app.ts`, `runtime.ts`, `database/migrations.ts`, `infra/postgres/grant-runtime-privileges.mjs`                                       | Both feature registrations, runtime dependencies, real 0019 then real 0020 and minimal runtime grants. No placeholder or 0021 is introduced here.                                                                                                     |
| `apps/web/src/lib/server/memory-page.ts`                                                                                                           | Current route group validation before create; strict source/group result validation; non-enhanced POST search with exact action marker only.                                                                                                          |
| `apps/web/src/lib/conversation-pending-command.ts`, `apps/web/src/routes/app/workspaces/[workspaceId]/conversations/[conversationId]/+page.svelte` | Current live Save mapping plus one independent bounded Save/edit intent snapshot. Retain original key and exact source/version/confidence/body through lost responses and stream reset until acknowledgment/discard.                                  |
| `apps/web/src/lib/conversation-message.ts`, stream client/BFF/routes                                                                               | Incoming pure current-message parser and full COL-05 delivery protocol remain authoritative; no duplicate parser is added.                                                                                                                            |
| `apps/web/tests/e2e/fixture-api.mjs`, `conversation-stream-fixture.mjs`, memory/stream/retry scenarios                                             | Both real API feature registrations and actual live human/Bot Save, no-JS search, current-source and committed-response-loss evidence.                                                                                                                |
| `.github/workflows/verify.yml`, `infra/verify-memories.mjs`, `infra/verify-conversation-stream.mjs`, `infra/verify-task-provider.mjs`              | Both dedicated native jobs, actual migration expectations and Compose stream/memory/provider checks retained.                                                                                                                                         |
| API migration/storage/native Task tests                                                                                                            | Actual predecessor selection before 0019, deferred tail guard checks, true sole Bot output identity and ordering after trigger rather than fixed sequence assumptions.                                                                                |

## External and integration gates still open

- MEM PostgreSQL suite: **14 authored/registered and 14 skipped locally** without `TEST_MEMORY_DATABASE_URL`. These results do not prove PostgreSQL locks, guards, least privileges, concurrent idempotency or rollback. The dedicated CI service/runtime-role job must execute them.
- `infra/verify-memories.mjs` and the inherited stream helper have syntax-check evidence only here. Their real Compose API/runtime-role smoke remains external. No PostgreSQL or Docker services were provisioned locally.
- Inherited ATT PostgreSQL/S3/Compose and COL-05 native execution remain owned by their existing external gates; this integration does not close them by inference.
- Root's dedicated merger must combine this pin with later routing/Task-history work, preserve the shared seams above, run the combined acceptance gates and obtain root acceptance. This author did not push, self-approve or update root ticket progress.
