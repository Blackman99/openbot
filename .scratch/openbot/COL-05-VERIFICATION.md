# COL-05 implementation verification

Original ticket22 and its five acceptance criteria remain unchanged. This branch began at accepted COL-04 `cb9977374ff2b3d40ebb9b5783647d99c32cfcb1` and incorporated accepted ATT-01 `0bbaf8562fc2727cc5c563f1f3a1555cdd910779` before registering actual migration0019. No predecessor migration was rewritten. Source acceptance, independent Standards/Spec reviews, dedicated merge, issue progress and publication remain root-owned.

## Acceptance evidence

| AC | Behavior | Concrete evidence |
| --- | --- | --- |
| 1 | A slow provider publishes text before terminal completion | `conversation-stream-worker.test.ts` holds the actual Worker provider after its first awaited callback and reads its committed delta. `conversation-stream.spec.ts` observes that draft through real production Fastify, services, Worker, BFF and browser while the Run is running and no Bot ledger output exists. |
| 2 | Last-Event-ID resumes strictly after applied events | Pure client tests interrupt a partial next frame and verify the second request sends the last applied cursor. Actual browser disconnects the API socket after sequence4, observes resumed cursor4, receives the next delta once and converges to one final message. Codec/BFF tests bound partial frames, queued frames, demand, drain and cancellation. |
| 3 | Current conversation permission controls opening, resumption and delivery | Reader tests authorize before cursor errors and re-admit session/current conversation. Actual browser removes current membership during an open stream and observes termination. Targeted-locator403 stops consumption without a fetch/ack loop. The40-case native suite separately covers observed PostgreSQL resource/session/authority waits and drain boundaries; those native cases are authored but not executed locally. |
| 4 | The final stream and ledger contain one answer | Callback terminal reconstruction is pure; a successful finish creates one Bot ledger row/reference before completed state. Browser replaces the Run preview with a single stable messageId. Final error/rejection cases clear preview state without inventing a permanent answer. |
| 5 | Text and Task/Run states use one durable ordered sequence | Trigger, queued, running, delta, Bot output and completed all allocate from one conversation sequence. Typed writers derive persisted Task/Run identities, maintain permanent transition receipts through retention, and keep audit/state/feed atomic. Native mutation/rollback/floor tests remain an explicit external gate. |

## Witnessed RED and corrections

- Missing protocol/client modules and absent first-delta durable delivery were exercised before their implementation.
- Retaining only reclaimable transition events originally allowed a repeated queued writer to allocate again after cleanup; the regression failed with tail3 instead of2. Permanent text-free transition receipts now fence that duplicate.
- Late delta and late final writes originally committed after a simulated mandatory-write wait crossed the deadline. Both public-seam regressions failed, then passed with final deadline sampling and whole-transaction rollback. Their pg-mem snapshot adapter verifies application rollback control flow only; actual atomicity belongs to the native barrier cases.
- A final source review found that a legacy allocation could update the conversation tail without touching any delivery table and therefore evade the deferred prefix check. The new mandatory tail-trigger installation case failed before adding the trigger, then all19 focused storage/worker/deadline/migration cases and API typecheck passed. The40th native case asserts the missing-delivery allocation rolls back unchanged and a following current writer appends exactly its matching reference; actual PostgreSQL execution remains external.
- A current purge marker at the same immutable version event originally re-entered bootstrap. The regression failed before accepting monotonic current deletion without permitting resurrection.
- The first real browser run exposed an idle-open flush delay: the BFF/Node response had no first body chunk until a provider event or later heartbeat. An immediate authorized API comment and BFF-generated data-free heartbeat now flush the connection; partial frames remain unacknowledged.
- The full API gate initially hit the default5s test deadline in two75-delta aggregate fixtures and the9-active-run fixture. Their standalone assertions already passed. Only these bounded large-fixture test deadlines were set explicitly (20s/15s), after which all373 API integration cases passed; no production timing was relaxed.

## Local checks

- Formatting and API/Web type checks passed; Svelte reported0 errors and0 warnings.
- API unit110, API integration373, Web unit121, Web integration591: **1195 non-browser cases passed** before the final tail guard. The added mandatory-installation case then passed in the19-case focused run, giving1196 distinct non-browser cases exercised; this is not a second full-suite run.
- Full ordinary browser suite40 and OIDC1 passed. After the final pagination/cache adjustment, the focused stream file passed5/5, including the new20-reference→ordinary30-message page regression. This gives41 distinct ordinary scenarios exercised, with the full40-suite result and later5-file result recorded separately. Both4399/4173 listeners were confirmed closed and released to root/MEM-01.
- API and Web production builds execute in the real browser gate. The stream fixture routes real IncomingMessage/ServerResponse through Fastify; it does not use buffered inject for SSE. Its database is pg-mem, so it proves the HTTP/BFF/browser behavior and does not claim native PostgreSQL locks or guards.
- The Compose smoke helper reported11 bounded extractor/transport checks and both running/reloaded orchestration fixtures passing with explicit network/database substitutes; the held provider first-chunk assertion was witnessed RED→GREEN. Parent integration checked both script syntaxes, formatting, workflow parsing, native-job URL/test selection, API/Web readiness and stage ordering. These checks do not execute Compose.
- Native discovery found40 cases in `conversation-stream-runtime.test.ts`; all40 were skipped because `TEST_CONVERSATION_STREAM_DATABASE_URL` is absent. No local PostgreSQL/Docker was started.
- Final static reconciliation of the inherited `tasks-runtime.test.ts` found two obsolete positive sequence expectations: direct submission tail1 and group-worker output4. Submission now proves one exact human trigger, its matching message delivery, one exact queued Run delivery/receipt and tail at that queued sequence. Final output is matched to the sole Bot ledger row scoped by conversation and Run. Three rejected raw writes now choose the next available sequence so they retain their identity/immutability/expiry guard intent. Other ledger-count, lock, transaction and native assertions remain intact. This was a static contradiction, not an observed native failure. API typecheck, formatting and7 focused Task/storage integration cases passed; native discovery found26 Task cases plus40 stream cases and skipped all66. Native execution remains external.

## Explicit remaining external evidence: COL-05-E1

`postgres-conversation-streams` must execute all40 native cases against its dedicated disposable PostgreSQL17 database, actual migration registry and deployed restricted role. These include observed source/session locks, post-drain authority, source-before-Task/Run ordering, forced mandatory insert/delete rollback, real deadline waits, immutable delivery/floor guards, conversation-tail allocations without delivery, prefix cleanup and one final output. Discovery or pg-mem is not a substitute.

The separate worker/API Compose gate must additionally run `infra/verify-conversation-stream.mjs` through the API container with `OPENBOT_STREAM_SMOKE_STAGE=running` before the existing Task running verification and `OPENBOT_STREAM_SMOKE_STAGE=reloaded` after existing Task history verification. Both stages now wait for API and Web readiness. The running stage asserts committed first text through BFF while the provider is held and cancellation leaves the same Run/provider call live; the reloaded stage resumes after that retained delta cursor, checks every ordered durable event and resolves exactly one final current ledger message. The fixture holds the provider while the worker survives API shutdown; it preserves the existing final response text. Local syntax/build/contract checks do not prove this external deployment gate. Baseline COL-04/ATT native closure remains independently owned and is not claimed by this ticket.

## Integration seams

- MEM-01 consumes actual0019 and attaches memory selection after target admission, references after successful claim CAS, and current-source checks inside `publishDelta` and `finishTransaction`. No memory selector or system-prompt change is implemented here.
- COL-09 consumes `appendQueuedRunState` and positive persisted attempt numbers. The existing migration manifest and output sequence test adaptations preserve exact ledger-row identity and order.
- COL-07 keeps the callback publication/terminal rebuild split and the final deadline checks; cancellation and cumulative retained partials are outside this ticket.
- Root's pending COL-06 routing summary is optional `{algorithm:'local-terms-v1',reason:'mention'|'default'|'local-match'}`. Its actual source integration must preserve that exact small summary in strict Task/stream projections without copying full candidate lists. This branch does not implement routing or its migration.
- Private routes only. API-06 can reuse the canonical conversation cursor, durable floor and framing later; no public `/v1/events` or workspace-global cursor is introduced.
