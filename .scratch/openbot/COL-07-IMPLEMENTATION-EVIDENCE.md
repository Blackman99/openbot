# COL-07 implementation checkpoints

Implementation branch: `ticket/col-07`, isolated from accepted root `e51fafe4f5de1e5f0f5b8e1f91dbbc93201143c8`. The [approved handoff](COL-07-APPROVED-HANDOFF.md) records root's authorization and migration/fixture rules. This is incomplete ticket work, not final acceptance. Root metadata is unchanged.

## First slice: observe a durable claim during silent generation

The first RED used the existing real TaskWorker and migrated pg-mem fixture. A provider adapter stayed silent while a separate valid Run/Task transaction ended its claim. The assertion expected the provider AbortSignal to become aborted within the observation window; it remained false and failed with `expected false to be true`. The deliberately late terminal response was then released for test cleanup.

The GREEN adds a read-only current Task/Run/token/latest-attempt/deadline query and one serialized worker observation loop. It checks before network admission and every 1,000 ms after a successful observation. Inactive claim or database observation failure aborts that Run's controller. The loop/timer is cleared and any in-flight read awaited on completion. Existing final admission remains authoritative; a late response cannot overwrite the terminal Run or add a Bot output. No schema, route, migration registry, provider protocol or browser configuration changed.

The initial correlated-subquery form was rejected by pg-mem (`column t.id does not exist`); the final query uses the same selected Task parameter in the maximum-attempt subquery and retains the latest-attempt condition. A public queue assertion checks live versus terminal claim observation. The test uses the repository's existing TypeScript library target and ordinary deferred Promises, with no tsconfig change.

Actual checks on 2026-09-05:

| Check                                                                      | Result                                                                                               |
| -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Untouched baseline `task-worker.test.ts` + `task-loop.test.ts`             | 13 passed.                                                                                           |
| First focused RED                                                          | Exit 1, one expected behavior assertion failed; 12 unselected cases skipped by the test-name filter. |
| Final worker + loop suite                                                  | Exit 0, 14 passed across two files.                                                                  |
| API source and tests TypeScript check                                      | Exit 0.                                                                                              |
| Changed source/test and approved handoff formatting; diff whitespace check | Passed.                                                                                              |

Logs are retained outside the repository as `col07-observer-red.log/.exit` and `col07-observer-green.log/.exit`. Existing installed dependencies are linked only in this isolated worktree; no dependency installation occurred. SvelteKit metadata was generated before the typecheck, without a browser or listening service.

This slice proves durable-state observation and AbortSignal delivery through the adapter interface. It does not yet prove the user cancellation endpoint, atomic subtree cancellation, durable partial storage, real HTTP socket abort, native PostgreSQL guards, or separate-process Compose behavior. The test's external producer makes an existing legal failed transition; it does not pretend an unregistered cancellation schema already exists. Final full two-axis review, integration, native and Compose gates remain required.

## Current coordination

COL-05 supplied development pins `6a8b9e88` (core/actual0019), `ff5db5d` (private reader/HTTP/cleanup) and `aaa5b19` (publication-deadline rollback); no final acceptance is inferred and none is imported into this first slice. COL-09 supplied checkpoint `497f366`, preserving immutable latest-attempt/receipt semantics and leaving0022 unregistered until actual0020/0021. The final accepted dependency chain will replace these development assumptions before0023 registration.

Next slices add command/schema and legal multi-level fixtures, ancestor/late-write fences, cancellation-specific durable partial output, strict Web behavior and actual HTTP abort evidence. Migration0023 must reject any existing running legacy Run after old claims/workers are stopped and drained; queued Runs remain for the upgraded worker. No old-prefix guessing/backfill is permitted.

## Development streaming dependency import

Imported the actual COL-05 source through `aaa5b19`, including0019, the typed delivery writers, callback publication, private reader/BFF and final publication-deadline rollback. This remains an unaccepted development dependency. The merge preserves the first COL-07 observer, accepted API-02/attachment source and the Task response locator. Its only textual conflict was the fixture return value: the merged helper exposes both root's `auth` and streaming's `database`. The shared queue/worker retain the final progress/retention deadline check and private `finishTransaction` rollback path.

Actual verification: formatting passed; API types passed; Web types reported zero errors and zero warnings; all API unit/integration tests passed **495/495** and all Web unit/integration tests passed **706/706**; API and Web builds passed. No browser, native PostgreSQL or Compose execution was attempted. The log is `col07-import-stream-verification.log` with a successful build exit recorded in `.log.exit`. This is a composite command record: the initial `pnpm` wrapper stopped before checks on its dependency-status hook, and the direct build launch later found no API-local `tsc` binary; the final build used the installed workspace binaries with `pnpm_config_verify_deps_before_run=false`. Neither infrastructure interruption was a test failure or a dependency installation.

Root advised waiting for COL-09's composition of actual0021 before importing retry again. The isolated implementation can continue with pure command and observer slices;0023 remains unregistered and the real accepted predecessor chain is still required.

## Second slice: finish an aborted invocation and close the real request

The stronger RED kept the provider promise unresolved after its AbortSignal fired. The worker invocation still waited for that non-cooperative adapter, failing `workerFinished` at the bounded assertion. The GREEN uses the existing transport `withAbort` helper to consume late promise outcomes and returns from the invocation after abort; callbacks first check the aborted signal before touching accumulators or publication. A deliberately late callback rejects, and the delayed terminal result cannot replace the retained terminal state or create Bot output. Logs: `col07-late-provider-red.log/.exit` and `col07-late-provider-green.log/.exit`.

The affected worker, loop, stream publication and deadline suites passed **17/17**. Two additional actual HTTP tests passed: a separate terminal transaction closes the production adapter's request both before the first delta and after an awaited, committed multibyte delta. Neither response had completed when the server observed its connection close. The same TaskWorker then handled a separate queued Task and published exactly its answer. These tests listen on ephemeral loopback ports and close their server/connections; shared browser ports were unused. API types and changed-file formatting passed.

These **19** cases prove worker/transport behavior over the current legal failed transition while0023 is unregistered. They are not evidence that the user cancellation endpoint, atomic subtree transaction, durable cancelled partials, real PostgreSQL guards or separate-process Compose scenario have been completed.

## Third slice: queued cancellation command and authority

The API RED called the declared cancellation route through real Fastify/domain fixtures and received404 instead of the expected200. The GREEN adds the private origin/session-checked route, exact cancellation command parsing, current conversation inspection, root/Task/current-Run lock ordering, stable receipts, cancelled states, an immutable-marker table draft, audit and typed cancelled delivery in one service transaction. Original execution humans and current group owners/admins can stop; the execution human in the Task stays unchanged. A repeated exact key returns its original receipt. A distinct key against the same cancelled Run records a zero-effect receipt without another state/audit/delivery transition. Completed/failed history remains unchanged.

Actual cancellation coverage is **10/10**: queued provider-call prevention; replay/no-op/stale-Run semantics; unavailable provider; invalid body/origin/session and unrelated direct access; completed/failed history; all three approved group authorities after grant closure, provider disablement and Bot archive; no direct Bot ACL requirement; current group role/membership reauthorization. The affected cancellation/Task/worker/stream storage/publication/deadline gate passed **32/32** across six files. API source/test types and changed-file formatting passed. Logs: `col07-queued-cancel-red.log/.exit`, `col07-queued-cancel-green.log/.exit`, `col07-queued-cancel-focused.log/.exit`.

The schema is deliberately **standalone and incomplete** at this checkpoint. Tests explicitly install its tables and named status checks after the existing fixture migration. pg-mem uses different names for the five old unnamed status CHECKs, so the fixture replaces those exact emulator checks; it does not pretend PostgreSQL triggers, locks or rollback work in the emulator. No0023 ledger entry, native guard installation, runtime grants, cancelled Web contract, partial-output persistence or tree/concurrency acceptance is claimed. Error classes move to a narrow shared module while the existing service exports remain compatible.

Root has now authorized development composition of actual COL-09 `4c025e593cb2db80c767abe74896bac7344bd1c7` (real0019→0022) and COL-05's final source, then isolated0023 registration. Those checkpoints still require final dependency corrections/reviews; completion metadata and acceptance stay with root.

## Actual retry/memory/routing dependency composition

Merged development pin `4c025e593cb2db80c767abe74896bac7344bd1c7`. Eight shared conflicts were resolved by retaining stream + memory app registration; actual0019→0020→0021→0022 definitions and exact migration expectations; cancelled + retry + routing routes; bounded attempt history and submission/routing admission; memory validation before delta/finish; the observer, abort-aware generate promise and callback fence. The queue delta versus that dependency is only the current-claim observer. The private final-deadline transaction and memory manifest checks remain present.

All **1,336** nonbrowser cases passed: API130 unit +442 integration, Web123 unit +641 integration. Formatting and types passed, including Web zero errors/zero warnings. The declaration build exposed an author error: the new public cancellation receipt interface was not exported. Exporting the command/receipt types fixed declaration emission; sequential API and Web builds then passed. The initial combined command therefore exited1 at build, followed by successful build verification; this is not a literal full `pnpm verify` exit0. The composite record is `col07-import-retry-verification.log` outside the repository. No browser or native PostgreSQL gate was run.

The final COL-05 dependency supplied by root is `9425c6647869668bc6de9112349d69219cd40131`; it still must be composed before0023. Preserve its tail guard and corrected native Task sequence assertions alongside COL-09's40 authored native cases. Their actual PostgreSQL execution and independent acceptance remain separate gates.

## Full COL-05 development dependency integration

Imported final development pin `9425c6647869668bc6de9112349d69219cd40131` after actual COL-09 chain `4c025e593cb2db80c767abe74896bac7344bd1c7`. Neither dependency import is ticket acceptance. Resolved the native Tasks conflicts by retaining the accepted dynamic audit baseline and all 40 cases while adopting exact delivery/receipt sequence checks and current Run output lookup through the runtime pool. The browser fixture retains public Bot and memory handlers, adds the stream handler/reset, and preserves the accepted public-to-status reset boundary. Compose's exact migration expectation now follows the actual registry through `0022_failed_task_retries`; it had stopped at 0019 in the incoming branch.

Verification at the composed source: formatting, sequential API/Web typechecks (Web 0 errors/0 warnings), all 1,345 nonbrowser tests (API 130 unit + 445 integration; Web 128 unit + 642 integration), and sequential API/Web builds passed. The full command batch exited 0; log `/workspace/scratch/2bc98607b3a9/col07-import-full-stream-verification.log`, exit file `.exit`. Browser, native PostgreSQL and Compose jobs were not run. The observer, late callback fence, final publication deadline guards, MEM checks, routing and immutable retry/latest Run behavior remain present.

## Registered 0023 cancellation and durable partial core

Actual predecessor chain now ends in `0022_failed_task_retries`; the new `0023_task_tree_cancellation` adds immutable root/parent/depth, command and Run markers, and a separate bounded `task_run_partial_outputs` checkpoint. The native preflight takes table locks, refuses any running legacy Run, and only then replaces the old status checks/Task guard around root backfill. Operators must stop new claims and drain old workers before upgrading; queued Tasks remain queued roots. No partial prefix is guessed or backfilled from an expiring feed. Root self-reference is a native deferred FK because pg-mem cannot validate a new self-referencing row in its insert path.

The typed delta writer stores its exact cumulative UTF-8 prefix in the same transaction as delivery and progress, before retention and the final deadline check. Successful canonical output removes that checkpoint in the completion transaction. Cancelled output remains private, retained and immutable. The exact-scope GET `/tasks/:taskId/runs/:runId/partial-output` only serves cancelled Runs after current conversation inspection; noncancelled state conflicts, foreign scope is forbidden, and denied valid sessions are retained. Failed partial access remains outside this ticket's current contract.

Root and ordered ancestor locks now precede current Run locks in claim, delta, finish and retry. New retry after cancelled ancestry is fenced; an already saved retry receipt still replays under its existing current-authority/model rules. The cancellation command traverses terminal intermediate Tasks, cancels all unfinished current descendants, leaves terminal histories and unrelated roots intact, and emits one transition/audit per affected Run. The synthetic child fixture creates a distinct authorized human trigger, exact hash/grant, queued first Run and receipt in one transaction; it never reparents or reuses a parent's trigger and does not claim delegation implementation.

Meaningful RED/GREEN: cancelled partial GET was 404, then passed after checkpoint/reader implementation; the multi-level fixture initially created a new retry beneath cancelled ancestry, then rejected it while preserving the prior exact receipt. The two real HTTP silent-provider cases now call the actual cancellation service, observe the socket close within their existing 1,500ms assertion and process a later independent Task. Only the setup-heavy six-Task/tree/retry case gets a documented 15s case budget after its default5s overrun under the broad batch; production observation/deadline limits are unchanged. Inherited aggregate stream fixtures now use legal 32,000-character output prefixes while preserving both UTF-8 and escaped-JSON truncation checks.

Current core verification: formatting, API typecheck/build and all 1,348 nonbrowser tests pass (API 130 unit + 448 integration; Web 128 unit + 642 integration), full batch exit0 in `/workspace/scratch/2bc98607b3a9/col07-cancellation-core-full.log` with sibling `.exit`. Earlier type error, over-limit fixture failures and case timeout remain recorded in `col07-cancellation-core-verification.log`; targeted partial/tree RED/GREEN logs are separate. Native functions, grants, upgrade refusal, row waits, rollback and Compose are authored source only and have not been executed. Web cancellation/interrupted UI, native test cases and Compose witness are still pending; this is a development checkpoint, not COL-07 acceptance.

## Independent guard finding: queued cancellation must retain NULL claim fields

Independent Standards pre-review at core `6af570cce741587988122f1d5f0a8e23243e9dad` found a static PostgreSQL guard gap: a runtime writer with a legitimate command/marker could attach a complete pinned provider/claim tuple while moving a queued Run to cancelled. The old guard froze that tuple only when the previous state was running. The correction compares started time, claim token, deadline and every provider field for **all** cancellations, including queued NULLs, alongside the existing usage/error/output and command-marker requirements. No service, DTO, permission or timeout changes are included in this correction.

The new dedicated native file first checks actual 0022 CHECK names and refuses a running upgrade without partial DDL or ledger changes, then drains that Run and verifies queued root preservation through actual0023. Its counterexample attempts the forged queued claim inside the complete legal receipt/marker/Task/audit/delivery transaction and checks rollback; the positive case checks unchanged NULL fields and no provider-adapter construction. These three native cases were discovered and skipped because `TEST_TASK_CANCELLATION_DATABASE_URL` is absent. This is a static finding plus authored native regression, **not an observed native RED/GREEN**. API typecheck passed. Logs: `col07-native-claim-counterexample-discovery.log` and `col07-claim-fix-types.log` outside the repository, each with an explicit exit file. More cancellation native races/rollback cases and the Compose/browser gates remain open.

## Web cancellation and interrupted output slice

The Web client strictly parses cancelled Task/Run state, exact cancellation receipts (including stable zero-effect replies), and a bounded UTF-8 partial DTO. A same-origin Task action keeps the original command key and expected Run after an unknown outcome; known conflicts require refreshing. Authority hints use the original execution human or current group owner/admin, independent of current model-use admission. The API remains authoritative and a denied valid session cookie is preserved.

Cancelled Task details render saved output escaped and explicitly interrupted, without a completed-message locator or human edit/delete controls. Live cancellation retains the already seen prefix and ignores late deltas; fresh stream bootstraps restore the durable cancelled prefix through the scoped private BFF, independent of stream retention. Restoration never submits or retries a Task and keeps aggregate preview limits. Empty partials and temporarily unavailable partial reads have distinct UI states.

Test-first slices failed on missing cancellation/partial client methods, unrecognized cancelled stream state, absent bootstrap restoration, missing action and missing UI. After implementation: Web 133 unit + 658 integration tests passed, with Svelte 0 errors/0 warnings. Logs: `col07-web-complete.log` and `col07-web-final-types.log`, exit0 files beside each; earlier focused RED/GREEN logs are retained outside the repository. The additional BFF tests initially expected no fixed Origin header and allowed expanded error fields; corrected fixtures now preserve the existing fixed-Origin/strict-error contract, with all6 passing. No browser or native runtime result is claimed at this slice.

## Native race and privilege cases authored

The dedicated cancellation suite now discovers 14 cases: actual CHECK-name/drain migration; forged queued claim rollback; legal queued no-adapter cancellation; exact replay/new-key zero-effect receipt; UTF-8 partial retention after feed reclamation and late writer rejection; a legal multilevel tree through terminal intermediates; whole-subtree audit failure rollback; real PostgreSQL wait barriers for claim/delta/finish; delta+partial+progress committing before a waiting cancel; both ancestor cancellation/failed-child retry orders; and exact runtime privilege/retained-identity checks. `pg_blocking_pids` confirms every concurrency barrier; elapsed sleeps do not establish ordering. The separate CI job requires a nonempty dedicated PostgreSQL URL before invoking the suite. Local discovery is **14 skipped**, not14 passed; API typecheck passed. Evidence: `col07-native-races-discovery.log` and `col07-native-types.log` with explicit exit0 files. Native runtime/Compose and actual browser checks remain open.

## Accepted four-ticket source composition

Merged accepted source `0ff6898eee671f04987fd5024a0bbc3c2d0afef4` into the cancellation branch after saving Web `19754f91dd87e61ae976e7339e6b1662beaa94da` and native `7418fce72af1565516853eaddfd3d2c0362c6141`. Nine conflicts were resolved by retaining the actual0023 registry/Compose exact list, both app services, current Run output lookup, routing summaries/details alongside cancelled state/partial UI, and both fixture handlers. The accepted memory snapshot/manifest, final deadline rechecks, callback fencing, current command replay and all prior routes remain intact; Task queue/service implementations needed no additional production merge change. The imported routing page SSR fixture adds the four required cancellation/partial defaults after Svelte reported missing PageData fields; all routing assertions are unchanged.

Formatting and API types passed; repaired Web types report0 errors/0 warnings. Focused composition checks pass29 API +91 Web. Initial type failure remains in `col07-accepted-merge-check.log`; corrected types are in `col07-merge-fixture-types.log`. Parent-stage blob maps and exact shared patches in both directions are recorded outside the repository as `col07-accepted-source-merge-stages.json`, `col07-accepted-source-merge-vs-ours.patch` and `col07-accepted-source-merge-vs-accepted.patch`. Browser fixtures/scenarios and Compose are continuing separately; no final acceptance is claimed.

## Actual browser witness and final cache correction

On merged source `aeb6560b3e4740bf287e4d901b8ad70131c53026`, the new fixture routes real incoming HTTP through the production Fastify app, domain services, TaskQueue/TaskWorker and production HTTP model adapter. A real loopback provider can stop before headers, publish a multibyte prefix and remain pending, or attempt a late completion. PostgreSQL is emulated and native guards are not installed; this is UI/transport evidence only. Fixture reset is scoped to every scenario, clears the active instance synchronously, and serializes closing its worker/provider/API connections.

The first fixture attempt used the wrong expected status for the existing `/__scenario` reset (200 instead of204); its exit1 is retained as fixture setup evidence. The next meaningful run passed all seven cancellation cases but failed one unchanged old stream assertion: the new cancelled link/copy had altered the running/expired preview DOM. The correction limits the Task link and interrupted-specific copy to cancelled execution state, preserving the old running-preview text and all assertions. The rerun passed **12/12**, exit0: seven cancellation cases and all five existing stream cases. Logs: `col07-browser-first.log`, `col07-browser-actual-first.log`, `col07-browser-green.log`, with explicit `.exit` files outside the repository.

Cases cover queued zero-provider execution; actual running socket abort and escaped interrupted text after reload/feed expiry; ordinary group viewer denial and current admin cancellation after provider/grant closure; membership revocation with a retained valid cookie; post-commit response loss and unchanged-key confirmation without another prompt/receipt; stale expected-Run conflict followed by explicit refresh; and a silent request before its first byte. The running case also revokes current partial-reader access and verifies the visible private prefix clears. No browser, production deadline or global test budget changed.

A separate meaningful reducer RED found that later execution history eviction removed the Bot identity and cancelled-state fence of still-visible interrupted output. The correction retains at most eight visible preview identities within the existing64 Run cache, evicts other records first, and removes only the evicted unavailable hint. Both active and terminal history overflow cases preserve author, interrupted text and the late-delta fence. Reducer/client checks pass **23/23**, exit0; the preceding assertion failure and GREEN are recorded in `col07-preview-identity-red.log` and `col07-preview-identity-green.log` with exit files.

## Native and Compose final source, not executed

Two further native cases cover both commit orders of overlapping ancestor/descendant cancellation, with `pg_blocking_pids` establishing the real lock wait. They assert three changed Runs, exactly one marker/audit/delivery transition per Run, two stable receipts, zero repeat changes, and no intermediate visible subtree state. API types pass; final discovery is **16 skipped**, not16 passed (`col07-native-final-types.log`, `col07-native-final-discovery.log/.exit`). The earlier independent queued-claim guard finding was closed by scoped Standards review at `c092d38f85e2646933e1d2a146bc1bb01c35b65e`; it was a static finding with an authored native counterexample, not observed native RED/GREEN.

The new independent `compose-task-cancellation` job uses actual fresh0023 and restricted runtime grants, a separate worker and real HTTP provider. Its seed/cancel/reloaded stages assert queued zero-call behavior, claim with API stopped, response abort within the existing1.5s observation allowance for streamed/silent requests, later independent completion, immutable private partial after25h stream cleanup and process restart, exact retained Task/Run/command counts, and rejection of deleting cancelled partials. JavaScript syntax and both YAML documents parse; all14 inherited jobs remain alongside two cancellation jobs. Docker, native PostgreSQL and Compose were not run locally.

The [operator/API contract](../../docs/task-cancellation.md) records current stop authority, atomic subtree semantics, partial retention and the mandatory stop/drain/refuse-running upgrade. The original five ticket AC and root completion metadata remain unchanged. Final composed local verification, whole-ticket two-axis review, independent integration, and actual native/Compose evidence remain required.

## First full gate and independent partial-reader finding

The full `pnpm verify` at source `6779a0356ff3ab3e06e8dcc56bd8cf716958155b`, tree `472031e772e7abe3b1cc89ebd0f1b6e16f8e449a`, exited1. Formatting, sequential API/Web types (Web0/0) and all **1,492** nonbrowser tests passed: API130 unit +455 integration, Web174 unit +733 integration. Ordinary browsers passed **59/60**, including all seven cancellation cases. OIDC and final build had not run when the command stopped. The sole browser failure was an inherited `safePage` assertion forbidding every cancellation button on a queued original-human Task. That state now correctly exposes one enabled exact **Cancel task** action. The helper correction applies that positive expectation only at the queued call site, retaining the zero-button checks on other pages and every privacy, receipt, prompt and final-response-locator assertion. Log `col07-final-verify.log/.exit` retains this literal exit1; it is not a full-gate success.

Independent Standards at the frozen source also reproduced a P2: a503 from cancelled partial details occurred before subscribing to or continuing SSE, producing repeated detail reads and zero event subscriptions. The author client tests observed RED for bootstrap503, later cancellation-event503, and a still-pending detail request. The correction moves partial restoration to one serial background queue with an independent30s deadline, records each attempted retained Run before I/O, preserves current interrupted text on transient failure, and does not alter the stream cursor. It rechecks current retained Run identity before applying a detail response. Fresh authorized bootstrap allows new attempts; manual Task details remain available. A401/403 still aborts the event reader, clears private state and terminates with the corresponding authorization status. Stopping or reconnecting cancels and awaits the independent request.

Focused checks include healthy SSE updates after both503 entry points, progress while a detail request is pending, actual request-signal cancellation,401/403 clearing, later successful cancelled detail after reconnection with the exact applied cursor, and no repeated request for the earlier503 Run. The first three failures are recorded in `col07-partial-degradation-red.log/.exit`. This correction requires its own frozen delta review and corrected browser/OIDC/build verification;6779 is not an accepted source.

The corrected client/reducer gate passes **29/29** and Web types report0 errors/0 warnings. Logs: `col07-partial-degradation-final-green.log`, `col07-partial-degradation-types.log`, combined successful exit in `col07-partial-degradation-final.exit`. No source/API/migration outside the Web client, its tests, the single queued-browser expectation and these contract/evidence documents changed for this correction.

## Corrected browser gate and inherited CI corrections

At `af4e59d3b08b483b9d2fc0d8fd474db3cbeed7df`, the repaired ordinary browser suite passed **60/60**, including all seven cancellation cases; OIDC passed **1/1**, followed by successful sequential API/Web builds. The combined command exited 0 in `col07-repaired-browser-build.log/.exit`. Both reserved browser ports were then confirmed closed and released. The original full `pnpm verify` exit 1 is retained above: these results form a corrected composite gate, not a later literal full-command exit 0. Independent Standards and Spec reviewed the partial 503 correction at this source and reported the scoped delta CLEAN.

Imported root's accepted `8691abecbd3d3a086f1bc9da331d5adfae8d2272` correction as five explicit `-x` commits ending in `11f0c0330f0be1a88de6081a233913dfb718488e`. This includes exactly four test/CI paths and two evidence documents: the Task native audit expectation/observer, memory native edit/purge fixtures, general Compose memory-audit observer, and the memory witness. All 14 inherited jobs match that accepted correction, except for this ticket's required actual 0023 suffix in the exact migration list; the two cancellation jobs remain additive. The prior current-Run output lookup is retained. The static composition check also compared inherited file blobs, parsed YAML and checked changed JavaScript syntax; it exited 0 in `col07-final-composition-static.log/.exit`. No unrelated completion metadata or domain implementation was imported.

Root verified the inherited correction on actual **Verify33965537394**, completed success at **2026-09-05T12:21:46Z**: 14/14 jobs, 253 native cases, 1,453 nonbrowser cases, 53 ordinary browsers, 1 OIDC, 14 object-storage cases and both Compose jobs. Its checkout `d83fa680` has tree `89a71e230bac00504399752670ff7e19c1b58260`, matching the published correction. This actual run closes the four inherited native/Compose gate blockers on that published tree; the imported CI-fix documents remain historical records. That run predates COL-07 and does **not** execute this ticket's new PostgreSQL or Compose jobs.

## Final UTF-16 native guard correction

Author inspection and independent Standards confirmed another static P2 in the unpublished actual 0023 guard: PostgreSQL `length(text)` counts Unicode characters, while the existing application and strict Web partial contract limit JavaScript UTF-16 code units. Thus 16,001 supplementary characters fit the old native 32,000-character and 128,000-byte guards but exceed the Web 32,000-unit limit. The correction adds one unit for each supplementary character, with explicit `COLLATE "C"` for the regex range, and raises a specific 23514 error above 32,000 units. Existing byte, contiguous-delivery, current-claim, latest-Run and deadline guards remain unchanged. The [PostgreSQL string](https://www.postgresql.org/docs/17/functions-string.html) and [pattern-matching](https://www.postgresql.org/docs/17/functions-matching.html) documentation informed the character-count and fixed-collation distinction.

The test-first SQL-expression check observed **4 failed / 2 passed** using the original native expression (`col07-utf16-sql-red.log/.exit`, exit 1). It evaluates the exported production expression with explicit PostgreSQL scalar-counting semantics; pg-mem's default JavaScript length would hide the defect. Six cases cover BMP, supplementary and mixed text at and immediately beyond the boundary. The emulator cannot parse `COLLATE`; the test removes only that annotation and supplies a fixed Unicode regex implementation. The native cases execute the full expression. This is executable expression evidence, not native PostgreSQL execution. The intermediate parser error and a subsequently corrected native-fixture TypeScript error remain in their original logs.

Two new native cases build otherwise legal contiguous raw delta, progress, checkpoint and retention transactions without the application's length guard. They retain 16,000 supplementary characters or 32,000 BMP characters, reject one further character with the specific UTF-16 error, compare the complete persisted snapshot after rollback, then cancel and read the full legal boundary prefix. Existing 16 native cases are unchanged. Final native discovery is **18 skipped**, never 18 passed. The source correction is pinned at `fb38a4e90702f03aeb7d55ded8400ad139c31929`, tree `eee325fc43e45d01179a13941f9a08eb2a56d858`; its only production change after the reviewed Web correction is this native guard.

Final affected verification passed **18/18 API cases** (six expression boundaries, ten cancellation cases, two partial cases), API types, API declaration build and formatting. Logs are `col07-utf16-focused-green.log`, `col07-utf16-final-types.log`, `col07-utf16-native-discovery.log` and `col07-utf16-api-build.log`; the corrected types/discovery/build batch exited 0 in `col07-utf16-verification.exit`. The earlier intermediate batch exit 2 is not reported as success.

The final author gate therefore consists of the original **1,492 passing nonbrowser cases and lint/types**, the corrected **29 Web client/reducer and 18 affected API cases**, **60 ordinary browsers + 1 OIDC**, and successful sequential builds, with the API build repeated after the SQL correction. The six new Web regressions and six new SQL-expression cases were executed in those focused runs; there was no fresh single full-suite total. Browser results are reused because the final native guard and imported test/CI corrections do not change their application paths. No PostgreSQL or Docker services were provisioned.

Whole-ticket final Standards/Spec review, independent integration and actual **18-case PostgreSQL cancellation plus separate-process Compose cancellation** remain required. The original five acceptance-criterion texts and root completion metadata remain unchanged; this evidence does not self-approve COL-07.
