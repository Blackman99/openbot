# COL-12 — Hierarchical execution limits preimplementation handoff

Planning only (English). The previous Chinese full draft was removed for repository language policy; recover it from git history if needed.

- Still blocked. Ticket: `issues/29-col-12-enforce-hierarchical-execution-limits.md`.
- Root-fixed directions to preserve when rewriting the English handoff: active-Run duration ms after lock wait; turns = successful new Runs; depth = parent edges from root; handoffs = committed Lead transfers; Workspace/Group caps are per-root templates, not shared daily quotas.
- Do not register migrations or claim AC from this stub.
