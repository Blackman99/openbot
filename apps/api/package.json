{
  "name": "@openbot/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "node --env-file-if-exists=../../.env --import tsx --watch src/server.ts",
    "migrate": "node --env-file-if-exists=../../.env --import tsx src/database/migrate.ts",
    "start": "node dist/server.js",
    "worker": "node --env-file-if-exists=../../.env --import tsx src/worker.ts",
    "test:integration": "vitest run tests/integration --maxWorkers=4",
    "test:postgres": "vitest run tests/postgres",
    "test:unit": "vitest run tests/unit --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.1127.0",
    "@fastify/cors": "11.3.0",
    "fastify": "5.12.3",
    "openid-client": "6.8.7",
    "pg": "8.23.0",
    "sharp": "0.35.4"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "@types/pg": "8.23.1",
    "pg-mem": "3.0.14",
    "tsx": "4.23.13",
    "vitest": "5.0.0",
    "yaml": "2.9.0"
  }
}
